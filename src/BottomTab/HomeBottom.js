import React, { Component } from "react";
import { Text, View } from "react-native";

export default class HomeBottom extends Component{
   render(){
    return(
        <View>
            <Text style={{fontSize:20,color:'black',fontWeight:'bold'}}> this is biology page</Text>
        </View>
    )
   } 
}