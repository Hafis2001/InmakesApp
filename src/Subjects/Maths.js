import React, { Component } from "react";
import { Text, View } from "react-native";

export default class Maths extends Component{
   render(){
    return(
        <View>
            <Text style={{fontSize:20,color:'black',fontWeight:'bold'}}> this is Maths page</Text>
        </View>
    )
   } 
}